# Email (user-facing result delivery)
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=

# KakaoTalk share SDK
VITE_KAKAO_APP_KEY=

# Payment provider: mock | toss | polar  (defaults to mock when unset)
VITE_PAYMENT_PROVIDER=mock

# Toss Payments — required when VITE_PAYMENT_PROVIDER=toss
# Dashboard: https://developers.tosspayments.com/
VITE_TOSS_CLIENT_KEY=

# Server-side Toss key (used by /api/confirm-payment Cloudflare Pages Function).
# NEVER prefix with VITE_ — must stay off the client bundle.
# Set in Cloudflare Pages dashboard → Settings → Environment variables.
TOSS_SECRET_KEY=

# Polar — required when VITE_PAYMENT_PROVIDER=polar
# Dashboard: https://polar.sh/dashboard
# Checkout URLs are generated per product in the Polar dashboard.
# Configure each product's Success URL to:
#   https://projectyoon.com/?polar_checkout_id={CHECKOUT_ID}
VITE_POLAR_INDIVIDUAL_REPORT_URL=
VITE_POLAR_COUPLE_ADDON_URL=
VITE_POLAR_CONSULTATION_URL=

# Server-side Polar key (used by /api/confirm-polar Cloudflare Pages Function).
# Generate at Dashboard → Settings → Developers → Personal Access Tokens.
# NEVER prefix with VITE_ — must stay off the client bundle.
POLAR_ACCESS_TOKEN=

# Optional: override Polar API base URL.
# Default is https://api.polar.sh (production).
# Set to https://sandbox-api.polar.sh for sandbox testing — pair with a
# sandbox checkout URL and a sandbox PAT in the same environment.
POLAR_API_BASE=
